{
  "name": "heybuddy-proxy",
  "private": true,
  "description": "Cloudflare Worker proxy for HeyBuddy (Windows port of farzaa/clicky). Mirrors worker/package.json so Cloudflare Workers Builds detects this as a Workers project at the repo root.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
